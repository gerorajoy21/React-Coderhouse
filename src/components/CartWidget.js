import React from 'react';
import { Icon } from "semantic-ui-react";

const CartWidget = () => {
    return (
        <Icon className="bg-light" disabled name="cart" />
    );
  };
  
  export default CartWidget;
